# Variables de entorno locales para desarrollo (Cloudflare / OpenNext).
# Copia este archivo como `.dev.vars` y completa tus valores.
# NO subas `.dev.vars` a Git (ya está en .gitignore).
#
# Ejemplo:
# MY_SECRET=valor
